name: Build Surgical De-Esser

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-2022

    steps:
      - uses: actions/checkout@v4

      - name: Configure
        run: cmake -B build -DCMAKE_BUILD_TYPE=Release

      - name: Build
        run: cmake --build build --config Release --parallel

      - name: Upload VST3 (Windows)
        uses: actions/upload-artifact@v4
        with:
          name: SurgicalDeEsser-Windows-VST3
          path: build/SurgicalDeEsser_artefacts/Release/VST3/**
